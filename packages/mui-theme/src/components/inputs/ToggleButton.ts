import type { Components } from '@mui/material';

const MuiToggleButtonStyles: Components['MuiToggleButton'] = {
    defaultProps: {
        disableFocusRipple: true,
        disableRipple: true,
        disableTouchRipple: true,
    },
};

export default MuiToggleButtonStyles;
