import { Button } from '@nivoda/ui/button';

function App() {
    return (
        <Button variant="contained" color="primary" size="large">
            Hello, World
        </Button>
    );
}

export default App;
